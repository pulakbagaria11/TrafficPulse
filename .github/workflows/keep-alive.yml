name: Keep Streamlit app awake

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sSf --max-time 30 https://trafficpulse.streamlit.app/ -o /dev/null
